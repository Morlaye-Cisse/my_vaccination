import 'package:flutter/material.dart';

class ParametreView extends StatelessWidget {
  const ParametreView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Page de parametre"),
      ),
    );
  }
}
